#include "utilidades.h"

int localizarSubcadena(char cadena[], char subcadena[])
{
  int i = 0;
  int j = 1;
  int flag = -1;
  bool encontrado = false;
  while(cadena[i] != '\0' && flag == -1)
  {
    if(cadena[i] == subcadena[0])
    {
      while()
      {
        
      }
    }
  }

  return flag;
}
